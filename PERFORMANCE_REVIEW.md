# 网站性能审查

审查日期：2026-09-16。范围：全部 5 个 HTML 页面、5 个 JavaScript 文件、3 个 CSS 文件、图片与字体资源，以及本地预览服务和现有测试。下文保留优化前的审查结果。

## 已实施的五项优化（2026-09-16）

- 缩略图共享准备任务：浏览器验证 72 张图只调用 72 次 decode；失败任务移出缓存，重试只重新准备失败图片。全量原图预热保持不变。
- 首页源码预览在离屏、文档隐藏和预览弹窗打开时停止逐帧滚动；返回后按原时间轴恢复。尺寸与字体变化时重新测量滚动范围。
- 几何动画分别判断视口状态与文档可见性，修复标签页恢复时启动离屏动画的问题。
- 头像使用优化后的无损 PNG：1,631,924 → 1,261,213 字节，减小约 22.7%。原图保留，正常头像及倒影均引用新文件。原 PNG 带有 gAMA/cHRM 色彩信息，WebP 实验在浏览器中存在舍入色差，因此没有采用。最终 PNG 保留色彩信息，Pillow RGBA 和 Edge Canvas 像素对比均完全一致。
- 游戏暂停后保留静止主画布，粒子动画继续运行；暂停切换和窗口缩放会刷新主画布。缩放后保留两次绘制，使 Canvas 重置后的线条状态与原先持续绘制一致。

实际浏览器回归覆盖：缩略图调用次数、图片失败后重试、原图打开与切换、弹窗关闭、离屏停止与恢复、可见性事件、头像像素一致性、游戏暂停/缩放/恢复以及中英文切换。现有预览服务测试通过。测试未引入快照基线、hash 校验或额外门禁。

## 结论

有明显优化空间，无须改版、替换技术栈、减少动画数量或降低图片画质。优先处理重复工作、离屏绘制和资源下载策略。

需要区分两个要求：

- 保持布局、内容、画质、动画轨迹与交互不变：可以优化内部计算、缓存和无损编码。
- 连首次点开任意照片的等待时间也保持不变：不能简单取消所有原图预加载。减少预下载会影响未缓存图片的加载时间，需要明确权衡。

## 实际执行与测量

使用项目自带的 `tools/preview.mjs` 启动本地服务器，在 Edge 153 无头浏览器、1440×900 视口、每页独立浏览器上下文中打开全部页面。没有网络限速。导航完成后等待约 7 秒，读取 Resource Timing，并统计动画回调与图片 decode 调用。另运行了游戏开始与暂停、离屏状态事件和局部关闭任务的诊断实验。

以下为已完成子资源请求的 encodedBodySize 总和，不含主 HTML、HTTP 头或后续操作；MB 使用十进制。这些数据不是线上首屏流量，也不是所有访客必然下载的流量。

| 页面 | 子资源数量 | 资源体积 | 主要观察 |
| --- | ---: | ---: | --- |
| 首页 | 86 | 8.58 MB | 没有相册组件，仍下载 72 张缩略图，共 6.27 MB |
| Works | 9 | 0.58 MB | 没有持续运行的 JS 动画回调 |
| Timeline | 11 | 0.65 MB | 初始更新后没有持续运行的 JS 动画回调 |
| Playground | 162 | 249.27 MB | 未点开照片即下载全部 72 张原图，共 242.02 MB |
| Falling Pulse 设置页 | 2 | 0.086 MB | 粒子动画持续运行，资源下载量很小 |

全部页面没有捕获到 JavaScript 运行异常。现有 `node --test tools/preview.test.mjs` 测试通过。没有进行真实移动设备、线上 CDN、慢网或 GPU 显存测量，不能据此承诺帧率提升百分比。

## 可优先做的等价优化

### 1. 合并相册重复的缩略图准备任务

位置：`photos.js:8`、`:45`、`:333`。

`warmPhotoCache()` 与 `loadPhotos()` 同时遍历全部缩略图并调用 `decode()`。浏览器实测为 72 个缩略图请求、144 次 decode 调用。浏览器可能共享内部解码结果，因此不能把它说成双倍下载或双倍解码内存。

可以让两个入口共享同一批图片准备 Promise，保留现有全部图片准备完成再显示、随机顺序、淡入与轮播行为。这样减少重复 Image 对象、调用与异步任务，而不改变原图预热策略。

### 2. 源码预览离屏时停止逐帧读写

位置：`script.js:971`。

首页源码预览在屏幕外仍不断读取 scrollHeight、clientHeight 和 textContent，并写入 scrollTop。实测观察窗口内执行了 812 次回调，当时预览不可见。

可在不可见时停止调度，重新出现时按原时间轴恢复应有的位置；缓存源码长度与滚动距离，在内容、字体和尺寸变化时重新计算。不能简单从头播放，否则会改变可见表现。弹窗遮盖预览时同理处理。

单次诊断中，停止该离屏任务后脚本时间从约 136 降到 123 ms/s；这仅支持“任务存在开销”，不代表稳定收益或整机 CPU 百分比。

### 3. 修复首页几何动画的可见状态组合

位置：`script.js:889`、`:909`。

IntersectionObserver 与 visibilitychange 共用并覆盖 isVisible。页面已经滚到画布外时，标签页恢复可见仍会把 isVisible 设为 true，重新开启离屏绘制。

诊断中，滚到页底后绘制原本停止；派发 visibilitychange 事件后，约 1.2 秒新增 145 次 draw 回调。该实验验证了事件处理路径，没有模拟真实操作系统切换标签页。

应分别保存“画布在视口附近”和“文档可见”，仅同时满足时调度；resize 与交互触发也遵守同一条件。保持原先的 rootMargin 与旋转时间推进规则。

### 4. 几何动画复用计算结果与对象

位置：`script.js:585`、`:771`、`:781`。

二次细分球体包含 162 个顶点、480 条边。每个顶点都计算同一组旋转角的三角函数，每帧还重建投影和边段对象。

可以每帧只计算一次旋转系数，并复用顶点和边段缓冲对象。保留坐标运算顺序、画线顺序、透明度、交互与脉冲公式；避免为了批量绘制改变透明线条的叠加结果。收益需在改动后测量，不应把运算次数减少直接等同于帧率提升。

### 5. 头像使用无损编码

位置：`assets/avatar/IMG_9119.PNG`、`index.html:96`。

原头像实际分辨率为 1024×1024，文件 1,631,924 字节。临时目录中的 lossless WebP 转换结果为 845,692 字节，约减少 48.2%；Pillow 解码为 RGBA 后逐字节比较一致。

可保留原尺寸进行无损编码，不必缩小图片或降低质量。正式替换前仍需核对色彩配置、透明通道以及目标浏览器中的正常与放大显示。本轮转换产物仅存在临时目录，未替换网站资源。

### 6. 游戏减少静态内容的重复工作

位置：`games/falling-pulse/app.js:1311`、`:1452`。

游戏每帧重建背景渐变并绘制轨道。暂停时停止 update，但继续绘制整张游戏画布和独立粒子画布；实测暂停后脚本负载没有明显降低。

可缓存尺寸不变时的背景渐变、固定几何参数；暂停且主画布内容不变时保留已绘制帧，尺寸或状态变化再重绘。粒子在暂停时仍会运动，因此若要求保持现有表现，不能直接停掉整个 loop。缓存分层也要保留透明叠加顺序和 DPR。

## 收益大，但会影响资源准备时机的优化

### 7. 调整首页与原图预加载策略

位置：`index.html:15`，`photos.js:45`、`:52`、`:104`、`:377`。

首页的 `if (!gallery) return` 在全量缩略图预热之后，故没有相册也会加载它们。把首页预热改为相册入口接近视口或有访问意图时启动，可避免未访问相册的用户消耗约 6.27 MB。

Playground 的两个原图 worker 限制了并发数量，但没有限制总下载量，也不会因相册折叠、离屏而停止队列。改成当前图片、相邻图片与明确悬停目标优先，可避免单纯浏览列表便下载约 242 MB。

现有查看器已经支持“先显示缩略图，再替换原图”，所以不用修改界面或照片质量。但取消全量预下载，可能延长随机打开未缓存原图时的等待；它不能归入严格的所有体验完全等价优化。

### 8. 随部署生成静态照片索引

位置：`photos.js:23`，`assets/photos/README.md`。

线上通过 GitHub API 获取 main 的完整递归文件树，且使用 no-store。可在部署时从同一目录规则生成 `assets/photos/index.json`，浏览器从网站自身读取。这样减少跨域请求、API 限流依赖及无关目录数据，并使索引对应实际部署的资源。

必须保留自动发现照片、自然排序、隐藏文件与符号链接排除等现有语义；本地预览仍需动态扫描目录。应将生成动作纳入实际部署流程，不能要求手工维护一个容易过期的文件列表。本轮未检查线上部署配置，未实施该项。

## 次要事项与不建议直接改动的部分

- 同一个 script.js 在首页使用 v=7，其他页面使用 v=6；languages.js 在 Timeline 使用 v=12，其他页面使用 v=7。同一静态文件会形成不同缓存 URL，可统一现有版本引用，无须新增 hash 或门禁。
- Space Grotesk 仍使用 TTF，可评估转 WOFF2 并保留所有字形与可变轴。字体总量远小于照片，优先级较低，本轮未测转换收益。
- CSS 存在大面积模糊、动态 background-position、阴影和玻璃效果。这些有潜在绘制成本，但仅暂停背景动画的诊断结果波动较大，未证明它是主要瓶颈。不能把移除模糊、简化背景或降低动画频率称为“表现完全不变”。
- 72 张缩略图共约 6951 万像素，最大一张达 2016×2688。重新生成更小缩略图可降低解码成本，但要先验证高 DPR 和大图预览清晰度，不属于未经验证的无损优化。
- 不建议为此引入前端框架、Service Worker 或复杂打包系统。当前静态结构本身没有明显框架开销。

## 已有优化应保留

- 脚本 defer、本地字体与 font-display: swap。
- 首页 Mod 图片在打开弹窗后加载；Playground Mod 图片使用 lazy loading。
- 照片轮播已在离屏、标签页隐藏、查看器打开和触摸交互时暂停。
- Timeline 已合并滚动更新到 requestAnimationFrame，读取位置后再写样式。
- 游戏已有可见音符起点、漏判和自动演奏游标、粒子对象复用，以及倒计时文本变化时才更新的处理。
- 现有 reduced-motion、焦点管理、目录访问限制和错误提示不应为优化而删除。

建议实施顺序：共享缩略图准备任务 → 修复离屏调度与状态组合 → 无损头像 → 几何计算复用 → 游戏静态绘制缓存。资源加载策略与静态索引作为后续独立改动，以照片准备速度和部署行为的实际验证决定。
